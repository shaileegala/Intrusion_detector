
# Validate username password
class User(object):

    def __init__(self, id, username, fname, lname):
        self.id = id
        self.username = username
        self.fname = fname
        self.lname = lname

    @classmethod
    def fetch_by_username(cls, username):
        query = ""

        # Execute Query
        # Fetch result

        # If anything
            # return User(<with all values put properly>)
        # else:
            # None


# TODO

# Get mean

# Update Total and Count